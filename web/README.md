# web/

React + Vite + Tailwind SPA source. Lands in Sprint 2 (story S2.3).

Build output goes to `web/dist/` (gitignored), which the Go binary embeds
and serves. Until then this folder stays a placeholder.
