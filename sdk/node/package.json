{
  "name": "@touchgrass/node",
  "version": "0.1.0",
  "description": "Fail-open error SDK for touchgrass: uncaught exceptions, manual reports, breadcrumbs, release tags.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "verify": "tsc --noEmit -p tsconfig.check.json && eslint . && vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
